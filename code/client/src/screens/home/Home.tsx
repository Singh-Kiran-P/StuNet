import React, { Screen } from '@/.';
import { View, Text } from '@/components';

export default Screen('Home', ({ nav }) => {

    return (
        <View>
            <Text children='TODO show subscribed courses and questions'/>
        </View>
    )
})
