import React, { Screen } from '@/.';

import {
    View
} from '@/components';

export default Screen('EditProfile', ({ params, nav }) => {

    return (
        <View>
            
        </View>
    )
})
