import React, { Screen } from '@/.';

import {
    View,
    Button
} from '@/components';

export default Screen('CreateAnswer', ({ params, nav }) => {



    return (
        <View>
            
        </View>
    )
})
