{
  "scripts": {
    "server": "concurrently \"docker-compose up\" dotnet run --project server",
    "client": "cd client && flutter run && cd .."
  },
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
