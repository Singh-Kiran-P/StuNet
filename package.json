{
    "scripts": {
        "server": "dotnet run --project server",
        "client": "cd client && flutter run && cd ..",
        "dev": "concurrently \"docker-compose up\" \"npm run client\" \"npm run server\""
    },
    "dependencies": {
        "concurrently": "^6.3.0"
    }
}
